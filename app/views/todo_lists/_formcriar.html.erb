<%= form_with(model: todo_list) do |form| %>
    <% if todo_list.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(todo_list.errors.count, "error") %> prohibited this todo_list from being saved:</h2>
  
        <ul>
          <% todo_list.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>
  
    <div class="d-flex">
      <div class="field">

        <%= form.text_field :title, class:"h-100", placeholder: "Título"%>
      </div>
    
      <div class="actions adjust-button ">
        <%= form.submit "Criar", class:"btn btn-logout text-center m-0"%>
      </div>
      <% end %>
    </div>
  
  
  